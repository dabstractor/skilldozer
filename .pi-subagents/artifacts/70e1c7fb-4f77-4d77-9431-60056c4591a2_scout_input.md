# Task for scout

Codebase recon for the skilldozer repo at /home/dustin/projects/skilldozer. Verify the test-breakage surface for a planned change with EXACT file:line refs (read the actual files; do not guess).

PLANNED CHANGE: (A) insert a new `findConfig()` rule into `skillsdir.Find()` between `findEnv` and `findSibling` (new order: env -> config -> sibling -> walkup). `findConfig` reads SKILLDOZER_CONFIG env (or default XDG config path) via config.Path(), loads via config.Load(), returns the `store` dir if it exists, else falls through (returns found=false). (B) change `skillsdir.ErrNotFound` message from "could not locate the skills directory: set $SKILLDOZER_SKILLS_DIR, cd into the skilldozer repo, or reinstall skilldozer" to "skilldozer is not configured; run `skilldozer init`".

REPORT (be exhaustive, exact file:line):
1. EVERY test function in EVERY *_test.go file that asserts the OLD ErrNotFound message or any of its substrings ("SKILLDOZER_SKILLS_DIR", "cd", "reinstall"). For each: name, file:line, exact assertion. Confirm what it must change to (new message has "run" and "skilldozer init", none of the old words).
2. EVERY test that calls skillsdir.Find() or run() and relies on "all rules miss -> ErrNotFound" by neutralizing ONLY SKILLDOZER_SKILLS_DIR (via unsetEnvVar in skillsdir_test.go or unsetSkillsEnv in main_test.go) without neutralizing the config rule. These become non-hermetic (would break on a machine with a real config). List each with file:line.
3. The exact definition (file:line) and body of the helpers `unsetEnvVar` (skillsdir_test.go) and `unsetSkillsEnv` (main_test.go) — do they neutralize ONLY the skills env var, or also SKILLDOZER_CONFIG?
4. Any non-test Go caller of skillsdir.Find() or skillsdir.ErrNotFound beyond main.go (check internal/resolve, internal/discover, internal/check, internal/search, internal/ui) — confirm behavior unchanged by the message flip.
5. Whether main.go prints ErrNotFound verbatim (fmt.Fprintln(stderr, err)) with no prefix at all Find() call sites, and whether --path uses src.String() via fmt %s (so the new "config file" label renders with zero main.go change).

Return a compact findings list. Read-only; modify nothing.

---
**Output:**
Write your findings to exactly this path: /home/dustin/projects/skilldozer/.pi-subagents/artifacts/outputs/70e1c7fb-4f77-4d77-9431-60056c4591a2/context.md
This path is authoritative for this run.
Ignore any other output filename or output path mentioned elsewhere, including output destinations in the base agent prompt, system prompt, or task instructions.

## Acceptance Contract
Acceptance level: reviewed
Completion is not accepted from prose alone. End with a structured acceptance report.

Criteria:
- criterion-1: Implement the requested change without widening scope
- criterion-2: Return evidence sufficient for an independent acceptance review

Required evidence: changed-files, tests-added, commands-run, validation-output, residual-risks, no-staged-files

Review gate: required by reviewer.

Finish with a fenced JSON block tagged `acceptance-report` in this shape:
Use empty arrays when no items apply; array fields contain strings unless object entries are shown.
```acceptance-report
{
  "criteriaSatisfied": [
    {
      "id": "criterion-1",
      "status": "satisfied",
      "evidence": "specific proof"
    }
  ],
  "changedFiles": [
    "src/file.ts"
  ],
  "testsAddedOrUpdated": [
    "test/file.test.ts"
  ],
  "commandsRun": [
    {
      "command": "command",
      "result": "passed",
      "summary": "short result"
    }
  ],
  "validationOutput": [
    "validation output or concise summary"
  ],
  "residualRisks": [
    "none"
  ],
  "noStagedFiles": true,
  "diffSummary": "short description of the diff",
  "reviewFindings": [
    "blocker: file.ts:12 - issue found, or no blockers"
  ],
  "manualNotes": "anything else the parent should know"
}
```